Flask==3.1.3
duckdb==1.5.3
# /methodology, /summary and /geo-correlation run statistical tests;
# /behavior/<router_hash> renders its charts on the fly.
pandas==3.0.3
scipy==1.17.1
matplotlib==3.10.9
# DuckDB needs pytz to convert the timezone-aware timestamps on /router/<hash>
# and /summary. pandas 3.0 no longer pulls it in, so pin it explicitly --
# without it those two pages 500 on a machine that lacks it.
pytz==2026.2
