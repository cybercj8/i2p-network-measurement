# Intentionally empty: this i2pd instance is a passive research vantage
# point only (netDb comparison against the Java I2P router already running)
# -- it doesn't need any client tunnels, and the default tunnels.conf's
# sample IRC tunnel binds a local port the Java router already uses.
